{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build project",
            "type": "shell",
            "command": "g++ -std=c++20 src/main.cpp -o dist/main",
            "windows": {
                "command": "g++ -std=c++20 src/main.cpp -o dist/main.exe",
            }
        }
    ]
}